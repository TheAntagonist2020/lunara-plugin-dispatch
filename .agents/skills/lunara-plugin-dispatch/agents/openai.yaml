interface:
  display_name: "Lunara Plugin Dispatch"
  short_description: "Repo-specific patterns and workflows for lunara-plugin-dispatch"
  default_prompt: "Use the lunara-plugin-dispatch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true